<!-- 改编自：人文关怀主题项目提示词/lp-135/公益捐赠追踪平台.md；后端由 未指定技术栈 改造为 Go 1.22 + Gin + GORM -->

请根据以下要求生成项目代码和 README。

## 项目类型：全栈Web应用

## 项目概述
构建一个公益捐赠追踪平台：公益组织注册后发布公益项目（目标金额、执行计划），项目审核通过后展示在首页并按助学、助老等分类筛选；用户注册后可选择项目捐款付款并生成电子凭证；项目页实时显示筹款进度和每一笔捐赠记录；项目方定期上传执行进展，捐赠人可在项目动态中查看；个人中心可查捐赠记录和累计金额；平台提供按捐款金额和服务时长排行的公益排行榜。

## 功能需求

1. **公益组织与项目管理**：公益组织注册登录后可发布公益项目，填写项目名称、分类（助学、助老、环保、救灾等）、目标金额、执行计划、图文介绍；项目提交后由平台管理员审核，审核通过才展示在首页；支持按分类筛选项目列表，支持项目编辑与上下架。

2. **用户注册与捐款**：普通用户注册登录后可浏览项目并选择捐款，填写捐款金额，选择支付方式（模拟支付/在线支付）完成付款；付款成功后生成电子凭证（凭证编号、项目名、金额、时间、捐款人），支持凭证查询与下载。

3. **项目页筹款进度与捐赠记录**：项目详情页实时显示筹款进度（已筹金额/目标金额、进度百分比、剩余天数），实时展示每一笔捐赠记录（金额、时间、匿名或昵称）；筹款达到目标金额后项目标记为已完成筹款。

4. **项目执行进展与动态**：项目方在项目执行过程中定期上传执行进展（文字、图片、进展节点、完成度），进展发布后进入项目动态时间线；捐赠人可在项目页动态中查看执行进展，平台记录进展发布时间与内容。

5. **个人中心**：用户可查看自己的捐赠记录列表（项目、金额、时间、凭证）和累计捐款金额，支持按时间范围筛选；可查看自己参与项目的执行动态更新。

6. **公益排行榜**：平台提供公益排行榜，按累计捐款金额排行，同时按志愿服务时长排行，展示排名、用户昵称、金额或服务时长；支持按周/月/全部时间范围切换。

## 核心实体

- **User**：用户（公益组织/普通用户/管理员），字段含账号、密码哈希、角色、昵称、头像、联系电话、注册时间。
- **CharityOrganization**：公益组织档案，字段含组织名称、简介、资质证照、联系方式、审核状态。
- **Project**：公益项目，字段含组织ID、名称、分类、目标金额、已筹金额、执行计划、图文、审核状态、状态（筹款中/已完成/已下架）。
- **Donation**：捐赠记录，字段含用户ID、项目ID、金额、支付方式、支付状态、凭证编号、捐赠时间。
- **DonationReceipt**：电子凭证，字段含凭证编号、捐赠ID、项目名、金额、用户昵称、生成时间。
- **ProjectProgress**：项目执行进展，字段含项目ID、标题、内容、图片、完成度、发布时间。
- **VolunteerService**：志愿服务时长记录，字段含用户ID、项目ID、服务时长、服务时间（用于排行榜服务时长统计）。
- **AuditRecord**：项目审核记录，字段含项目ID、审核人、审核结果、意见、审核时间。

## 核心页面

1. **首页**：展示通过审核的公益项目，按助学、助老、环保、救灾等分类筛选；项目卡片展示名称、分类、已筹/目标金额、进度条、剩余天数，点击进入项目详情页。

2. **项目详情页**：展示项目介绍与执行计划、实时筹款进度（已筹金额/目标金额、进度百分比）、实时捐赠记录列表、项目执行动态时间线，提供捐款入口。

3. **捐款支付页**：选择捐款金额（预设档位或自定义）、选择支付方式（模拟支付），支付成功后展示电子凭证并支持下载。

4. **个人中心**：展示用户累计捐款金额、捐赠记录列表（项目、金额、时间、凭证入口）、参与项目的动态更新。

5. **公益排行榜**：按累计捐款金额与志愿服务时长两个维度排行，展示排名、昵称、金额/时长，支持周/月/全部时间范围切换。

6. **公益组织后台**：项目发布与编辑（目标金额、执行计划、图文）、执行进展上传、项目状态管理。

7. **平台审核后台**：项目审核列表（通过/驳回）、项目上下架、组织资质审核。

## 技术要求

- **英文短名（用于 Docker Compose）**：`gbdonation`
- **前端端口**：8235（Docker 内部使用 80，由 Nginx 托管）
- **后端端口**：3235（Docker 内部统一使用 8080）
- **数据库端口**：5735
- **前端框架**：Vue 3 + TypeScript + Vite
- **UI 库**：Element Plus
- **后端**：Go 1.22 + Gin + GORM
- **数据库**：MySQL 8.0
- **认证**：JWT（github.com/golang-jwt/jwt/v5）+ RBAC（管理员/公益组织/普通用户角色）
- **密码哈希**：golang.org/x/crypto/bcrypt
- **参数校验**：github.com/go-playground/validator/v10
- **配置**：环境变量注入，github.com/caarlos0/env/v11
- **日志**：Go 标准库 log/slog，通过中间件输出结构化日志
- **API 文档**：github.com/swaggo/gin-swagger（挂载于 /docs）

## 后端目录结构（Go 标准布局）

后端代码放在 `backend/`，数据库脚本放在 `backend/database/`，禁止将 handler/service/repository/model 合并到单一文件：

```
backend/
├── go.mod
├── go.sum
├── Dockerfile
├── cmd/
│   └── server/
│       └── main.go            # 程序入口：加载配置、初始化 DB、注册路由、启动 HTTP 服务
├── internal/
│   ├── config/                # 配置结构体与加载逻辑（端口、数据库、JWT 等）
│   ├── model/                 # GORM 模型：User、CharityOrganization、Project、Donation、DonationReceipt、ProjectProgress、VolunteerService、AuditRecord 等
│   ├── repository/            # 数据访问层：UserRepository、ProjectRepository、DonationRepository、ReceiptRepository、ProgressRepository、RankRepository 等
│   ├── service/               # 业务逻辑层：AuthService、OrganizationService、ProjectService、AuditService、DonationService、ReceiptService、ProgressService、RankService 等
│   ├── handler/               # Gin HTTP 处理层：AuthHandler、OrganizationHandler、ProjectHandler、AuditHandler、DonationHandler、ReceiptHandler、ProgressHandler、RankHandler 等
│   ├── router/                # 路由注册：集中定义路由分组（公共/用户/组织/管理员）与中间件挂载
│   ├── middleware/            # 中间件：JWT 鉴权、RBAC、请求日志、统一错误处理、CORS 等
│   ├── dto/                   # 请求/响应结构体与参数校验标签
│   ├── constants/             # 错误码、项目分类、审核状态、项目状态、角色枚举等
│   └── util/                  # 工具函数：凭证编号生成、金额计算、排行榜聚合等
└── database/
    ├── init.sql               # 数据库初始化脚本
    └── seeds/                 # 模拟数据种子（分类、示例项目等）
```

前端代码放在 `frontend/`：页面（首页、项目列表、项目详情、捐款、个人中心、排行榜、组织后台、审核后台）、组件、路由、状态管理（Pinia）、API 请求、类型定义、样式与资源放在对应子文件夹。

## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。


## 部署要求

这是前后端分离项目，必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`：编排前端、后端、MySQL 8.0
- 根目录 `.env` 和 `.env.example`：包含 `COMPOSE_PROJECT_NAME=gbdonation`、`DB_NAME`、`DB_USER`、`DB_PASSWORD`、`DB_ROOT_PASSWORD`、`DB_PORT`、`BACKEND_PORT`、`FRONTEND_PORT`、`JWT_SECRET`
- `frontend/Dockerfile`：前端多阶段构建，先构建再用 Nginx 托管
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `backend/Dockerfile`：Go 多阶段构建
- `backend/database/init.sql`：数据库初始化脚本

backend/Dockerfile 必须使用 Go 多阶段构建：
```dockerfile
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server

FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

docker-compose.yml 要求：
- **不要写 `version:` 字段**（Compose V2 已废弃）
- 顶层声明 `name: gbdonation` 作为兜底，避免目录名为中文时项目名为空
- 所有服务使用 `container_name`，带 `${COMPOSE_PROJECT_NAME:-gbdonation}` 前缀
- 数据库数据使用命名卷持久化（顶层 `volumes:` 声明）
- 前端通过 Nginx 反向代理后端 API（前端代码调用 `/api`，Nginx 转发到 `http://backend:8080/`，不要硬编码 `localhost`）
- 服务间通过 Docker 内部网络通信，服务名即主机名
- MySQL 配置 `healthcheck`，backend 通过 `depends_on: { db: { condition: service_healthy } }` 等待数据库就绪
- backend 也配置 `healthcheck`（提供 `GET /healthz`），frontend `depends_on` backend 健康
- 暴露端口：前端 `8235:80`，后端 `${BACKEND_PORT:-3235}:8080`，MySQL `${DB_PORT:-5735}:3306`
- 支持 `docker compose up -d` 一条命令启动全部服务，不依赖额外的 `-p` 参数

Nginx 反向代理要求（`frontend/nginx.conf`）：
- `location /api/` 反向代理到 `http://backend:8080/`
- `try_files $uri $uri/ /index.html;` 支持前端 SPA 路由

## docker-compose.yml 参考骨架（务必遵循）

```yaml
name: gbdonation   # 顶层 name 字段兜底，避免中文目录名导致 project name 为空

services:
  db:
    image: mysql:8.0
    container_name: ${COMPOSE_PROJECT_NAME:-gbdonation}-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    volumes:
      - db_data:/var/lib/mysql
    ports:
      - "${DB_PORT:-5735}:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${DB_ROOT_PASSWORD}"]
      interval: 5s
      timeout: 5s
      retries: 20

  backend:
    build: ./backend
    container_name: ${COMPOSE_PROJECT_NAME:-gbdonation}-backend
    restart: unless-stopped
    ports:
      - "${BACKEND_PORT:-3235}:8080"
    environment:
      APP_ENV: production
      SERVER_PORT: 8080
      DB_HOST: db
      DB_PORT: 3306
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://localhost:8080/healthz || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 20

  frontend:
    build: ./frontend
    container_name: ${COMPOSE_PROJECT_NAME:-gbdonation}-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-8235}:80"
    depends_on:
      backend:
        condition: service_healthy

volumes:
  db_data:
```

`.env.example` 参考：

```dotenv
# Compose 项目名（小写字母/数字/-/_），避免中文目录名导致 project name 为空
COMPOSE_PROJECT_NAME=gbdonation

DB_NAME=app
DB_USER=app
DB_PASSWORD=app_pwd
DB_ROOT_PASSWORD=root_pwd

JWT_SECRET=change_me_to_a_long_random_string

FRONTEND_PORT=8235
BACKEND_PORT=3235
DB_PORT=5735
```

README 中务必告知用户首次启动前执行 `cp .env.example .env`。

## 构建验证清单（硬性约束）

- 后端必须能通过 `go mod tidy` 生成完整 `go.mod`/`go.sum`，依赖声明包括 `github.com/gin-gonic/gin`、`gorm.io/gorm`、`gorm.io/driver/mysql`、`github.com/golang-jwt/jwt/v5` 等
- 后端必须能通过 `go build ./...`；本地运行入口为 `go run ./cmd/server`
- 在 **中文目录名** 下执行 `docker compose config --quiet` 不报错
- 启动后 `docker compose ps` 显示全部容器 healthy，浏览器访问前端端口能看到页面且 `/api/...` 请求返回正常
- 禁止将 handler、service、repository、model、middleware、dto、constants 职责合并到单一文件
- 不得使用 `version:` 字段，Compose V2 顶层声明 `name: gbdonation`

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：项目类型标签（全栈Web应用）、项目名称和一句话介绍、项目主要功能、快速启动方式（Docker Compose 一键部署命令）、本地开发方式（前端启动命令、后端启动命令 `go mod tidy` + `go run ./cmd/server`）、访问地址、技术栈（表格展示，后端写 Go 1.22 + Gin + GORM）、项目目录结构（代码块展示）、主要 API 列表、环境变量说明、Docker 部署说明（端口映射、数据卷、常见问题）、License。

要求：Docker 部署命令放在最前面，作为首选启动方式；本地开发方式作为备选；命令可直接复制运行；告知用户首次启动前执行 `cp .env.example .env`；说明前端端口 8235、后端端口 3235、数据库端口 5735。

## Git 要求

代码编写完成后：
1. 在项目根目录初始化 Git 仓库（`git init`）
2. 创建 `.gitignore` 文件，至少包含：
   ```
   node_modules/
   dist/
   .env
   .DS_Store
   *.log
   .idea/
   .vscode/
   backend/bin/
   backend/server
   *.exe
   *.test
   coverage.out
   ```
3. 执行 `git add -A && git commit -m "init: 公益捐赠追踪平台初始化"`
